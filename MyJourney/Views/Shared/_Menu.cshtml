@using MyJourney.Controllers.Helper;

<ul class="nav navbar-nav">
    @foreach (var category in MenuHelper.GetMenu())
    {
        <li>@Html.ActionLink(category.Name, "WebsiteAbout", "Home")</li>
    }
</ul>
